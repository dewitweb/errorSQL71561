CREATE TABLE [auth].[tblApplication] (
    [ApplicationID]   INT          NOT NULL,
    [ApplicationName] VARCHAR (50) NULL,
    CONSTRAINT [PK_auth_tblApplication] PRIMARY KEY CLUSTERED ([ApplicationID] ASC)
);

